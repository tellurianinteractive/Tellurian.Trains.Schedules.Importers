@inject Translator Translator

@* Placeholder — the cargo flow editor (master rows per train, with FromCall/ToCall/position/description
   sub-items) is built in the next step. *@
<div class="cargo-trains">
    <p class="empty">@Translator("CargoTrainsComingSoon")</p>
</div>
