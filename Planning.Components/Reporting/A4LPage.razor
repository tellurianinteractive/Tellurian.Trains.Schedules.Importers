@inherits PageFormat

<div class="a4l" style="padding: @MarginStyle">
    @ChildContent
    @if (PageNumber > 0)
    {
        <div class="footer">
            @Footnote - <strong>@Translator("Page") @PageNumber</strong> -
        </div>
    }
</div>
