namespace Tellurian.Trains.Schedules.Model.Resources;

/// <summary>
/// Marker type identifying the <c>Notes</c> resx resources. Used as the resource source for
/// <see cref="System.Resources.ResourceManager"/> lookups of generated call-note text; the resx
/// entries are keyed by note kind (e.g. <c>CoupleToTrain</c>) with composite-format values.
/// </summary>
internal sealed class Notes;
